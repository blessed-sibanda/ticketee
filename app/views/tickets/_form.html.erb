<%= bootstrap_form_with(model: [ticket.project, ticket], label_errors: true) do |f| %>
  <%= f.text_field :name %>
  <%= f.text_area :description %>
  <%= f.file_field :attachment,label: 'File' %>
  <%= f.primary %>
<% end %>
