<div class="ticket">
  <h2><%= @ticket.name %></h2>
  <%= @ticket.description %>
</div>
<%= link_to 'Edit Ticket', edit_project_ticket_path(@ticket.project, @ticket) %>
<%= link_to 'Delete Ticket', [@ticket.project, @ticket], 
  method: :delete, data: {confirm: 'Are you sure?'} %>
